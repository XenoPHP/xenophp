{
  "$schema": "https://www.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "php xeno test && npx concurrently --kill-others --raw \"php xeno serve --internal\" \"php xeno socket:serve start\"",
    "socket": "php xeno socket:serve start",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "socket.io": "^4.8.3"
  }
}
